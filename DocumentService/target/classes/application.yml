eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 6004
spring:
  application:
    name: DocumentService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: niki_1604
    url: jdbc:mysql://localhost:3306/documentservice
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
aws:
   access-key: ${AWS_ACCESS_KEY}
   secret-key: ${AWS_SECRET_KEY}
   region: ap-south-1
   bucket-name:   nikita-loan-document 

   
